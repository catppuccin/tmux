---
whiskers:
  version: "2.1.1"
  matrix:
    - flavor
  filename: "themes/catppuccin_{{flavor.identifier}}.tmuxtheme"
---
{%- set palette = flavor.colors -%}
# vim:set ft=tmux:
# NOTE: you can use vars with $<var> and ${<var>} as long as the str is double quoted: ""
# WARNING: hex colors can't contain capital letters

# --> Catppuccin ({{ flavor.identifier | capitalize }})
set -g @thm_bg "#{{ palette.base.hex | lower }}"
set -g @thm_fg "#{{ palette.text.hex | lower }}"
set -g @thm_cyan "#{{ palette.sky.hex| lower }}"
set -g @thm_black "#{{ palette.mantle.hex | lower }}"
set -g @thm_gray "#{{ palette.surface0.hex | lower }}"
set -g @thm_magenta "#{{ palette.mauve.hex | lower }}"
set -g @thm_pink "#{{ palette.pink.hex | lower }}"
set -g @thm_red "#{{ palette.red.hex | lower }}"
set -g @thm_green "#{{ palette.green.hex | lower }}"
set -g @thm_yellow "#{{ palette.yellow.hex | lower }}"
set -g @thm_blue "#{{ palette.blue.hex | lower }}"
set -g @thm_orange "#{{ palette.peach.hex | lower }}"
set -g @thm_black4 "#{{ palette.surface2.hex | lower }}"
