# vim:set ft=tmux:
# NOTE: you can use vars with $<var> and ${<var>} as long as the str is double quoted: ""
# WARNING: hex colors can't contain capital letters

# --> Catppuccin (Frappe)
set -g @thm_bg "#303446"
set -g @thm_fg "#c6d0f5"
set -g @thm_cyan "#99d1db"
set -g @thm_black "#292c3c"
set -g @thm_gray "#414559"
set -g @thm_magenta "#ca9ee6"
set -g @thm_pink "#f4b8e4"
set -g @thm_red "#e78284"
set -g @thm_green "#a6d189"
set -g @thm_yellow "#e5c890"
set -g @thm_blue "#8caaee"
set -g @thm_orange "#ef9f76"
set -g @thm_black4 "#626880"
